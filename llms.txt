# Crate Documentation

**Version:** 0.4.0

**Format Version:** 57

# Module `nhs_number_cli`

# NHS Number command line interface (cli)

A National Health Service (NHS) Number is a unique ten-digit identifier
allocated to registered patients of NHS England and the NHS Isle of Man.

This tool is a command line interface that parses each standard input line
into an NHS Number, then validates that the check digit is correct.

References:

* [National Health Service (NHS)](https://en.wikipedia.org/wiki/National_Health_Service)

* [NHS Number](https://en.wikipedia.org/wiki/NHS_number)


## Modules

## Module `app`

```rust
pub(crate) mod app { /* ... */ }
```

### Modules

## Module `args`

Args for the application.

These args correspond to the matches in the file `clap.rs`.
We have these args in their own file in order to be flexible,
such as being able to start our app with other arg parsers.

```rust
pub(crate) mod args { /* ... */ }
```

### Types

#### Struct `Args`

```rust
pub struct Args {
    pub(crate) test: bool,
    pub(crate) log_level: Option<::log::Level>,
    pub(crate) check_lines: Option<bool>,
    pub(crate) counts: Option<bool>,
    pub(crate) column: Option<usize>,
    pub(crate) format: Format,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `test` | `bool` | Test flag that sets whether the app prints diagnostics.<br>Example: true means print diagnostics. |
| `log_level` | `Option<::log::Level>` | Log level: 0=none, 1=error, 2=warn, 3=info, 4=debug, 5=trace.<br>Example: 5 means print debug diagnostics. |
| `check_lines` | `Option<bool>` | Line validation subcommand: true=run, false=skip |
| `counts` | `Option<bool>` | Counts subcommand: emit a summary of valid / invalid /<br>parse-error / blank line counts to stdout instead of<br>per-line filtering. Mutually exclusive with `check_lines`. |
| `column` | `Option<usize>` | 1-based column index. When set, each input line is split on<br>`,` and the value at column N is taken as the candidate NHS<br>Number. When None, the whole line is the candidate. Honoured<br>by every subcommand that classifies lines. |
| `format` | `Format` | Output format selector. `Text` is the FR-10 stable contract<br>(`Error invalid line N. …` on stderr; key/value rows on stdout<br>from `--counts`). `Json` and `Tsv` are machine-readable<br>alternatives for downstream tooling. |

##### Implementations

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> Args { /* ... */ }
    ```

- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **Send**
- **Sync**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **UnsafeUnpin**
- **UnwindSafe**
#### Enum `Format`

Output format for diagnostics (line-validation subcommand) and
for the counts summary. Plain stdout from line-validation (the
canonical NHS Numbers) is always text and is not affected.

```rust
pub enum Format {
    Text,
    Json,
    Tsv,
}
```

##### Variants

###### `Text`

FR-10 stable text format. The default; do not change.

###### `Json`

One JSON object per diagnostic line (NDJSON). The counts
summary is a single JSON object.

###### `Tsv`

Tab-separated values. The line-validation diagnostic stream
is one row per failure with a fixed five-column schema
(kind, line_number, nhs_number, line, error). The counts
summary is one row of column headers followed by one row of
integer counts.

##### Implementations

###### Methods

- ```rust
  pub fn from_str(s: &str) -> Self { /* ... */ }
  ```
  Map the clap string value to a `Format`. Returns `Text` for

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Format { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> Format { /* ... */ }
    ```

- **Eq**
- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &Format) -> bool { /* ... */ }
    ```

- **RefUnwindSafe**
- **Send**
- **StructuralPartialEq**
- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **UnsafeUnpin**
- **UnwindSafe**
## Module `clap`

Command line argument parsing (CLAP) for the application.

clap is a crate for command line argument parsing.
See <https://docs.rs/clap/>

Clap has a variety of setup approaches:

  * via typical functions, which favors advanced uses yet is verbose.
  * via usage strings, which looks more like writing documentation.
  * via macros, which is fast and less verbose, yet atypical to read.
  * via YAML file, which favors localization and text file readability.

We prefer the typical functions, because they provide maximum capability,
and in our experience are the easiest for Rust IDEs to read and debug.

We favor our convention of doing clap setup in a file named `clap.rs`,
rather than in `main.rs`, because we like the separation of concerns.

```rust
pub(crate) mod clap { /* ... */ }
```

### Functions

#### Function `app`

Create a clap app.

```rust
pub fn app() -> clap::Command { /* ... */ }
```

#### Function `args`

Parse argv with the clap `Command` and project the matches into `Args`.

```rust
pub fn args() -> crate::app::args::Args { /* ... */ }
```

## Module `config`

Configuration for the application.

Our source code convention is using this file `config.rs` to
define `struct Config` and `impl std::default::Default`.

We manage configuration file settings via the `confy` crate.
See the project file `confy.rs` for testing our `confy` loading.

```rust
pub(crate) mod config { /* ... */ }
```

### Types

#### Struct `Config`

```rust
pub(crate) struct Config {
    pub(in ::app::config) version: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `version` | `u8` |  |

##### Implementations

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> Self { /* ... */ }
    ```

- **Deserialize**
  - ```rust
    fn deserialize<__D>(__deserializer: __D) -> _serde::__private228::Result<Self, <__D as >::Error>
where
    __D: _serde::Deserializer<''de> { /* ... */ }
    ```

- **DeserializeOwned**
- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **Send**
- **Serialize**
  - ```rust
    fn serialize<__S>(self: &Self, __serializer: __S) -> _serde::__private228::Result<<__S as >::Ok, <__S as >::Error>
where
    __S: _serde::Serializer { /* ... */ }
    ```

- **Sync**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **UnsafeUnpin**
- **UnwindSafe**
## Module `confy`

Configuration test of the `confy` crate for the application.

Our source code convention is using this file `confy.rs`
in order to test configuration file loading and parsing.

See also the project file `config.rs` for our `Config` struct.

```rust
pub(crate) mod confy { /* ... */ }
```

## Module `run`

Run the app

```rust
pub(crate) mod run { /* ... */ }
```

### Types

#### Enum `Error`

```rust
pub enum Error {
    Confy(::confy::ConfyError),
}
```

##### Variants

###### `Confy`

Fields:

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `::confy::ConfyError` |  |

##### Implementations

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Display**
  - ```rust
    fn fmt(self: &Self, __formatter: &mut ::core::fmt::Formatter<''_>) -> ::core::fmt::Result { /* ... */ }
    ```

- **Error**
- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **Send**
- **Sync**
- **ToString**
  - ```rust
    fn to_string(self: &Self) -> String { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **UnsafeUnpin**
- **UnwindSafe**
### Functions

#### Function `run`

Run everything.

Steps:

  * Initialize configuration.

  * Initialize arguments.


Example:

```
run();
//-> Initialize everything then run the subcommands.
```


```rust
pub(crate) fn run() -> Result<(), Error> { /* ... */ }
```

#### Function `dispatch`

Dispatch to a subcommand based on the parsed `Args`.

Default behaviour with no subcommand flag is the line validator,
so callers who do nothing more than `cat input.txt | nhs-number-cli`
keep working. Explicit flags pick a non-default subcommand:
`--counts` runs `subcommands::counts`, and `--line-validation` is
the explicit form of the default. The flags are mutually exclusive
at the clap layer; this function honours that by checking `counts`
first.

```rust
pub(in ::app::run) fn dispatch(args: &crate::app::args::Args) { /* ... */ }
```

#### Function `initialize_configuration`

```rust
pub(in ::app::run) fn initialize_configuration() -> Result<crate::app::config::Config, Error> { /* ... */ }
```

#### Function `initialize_arguments`

```rust
pub(in ::app::run) fn initialize_arguments() -> crate::app::args::Args { /* ... */ }
```

## Module `subcommands`

```rust
pub(crate) mod subcommands { /* ... */ }
```

### Modules

## Module `check_lines`

```rust
pub(crate) mod check_lines { /* ... */ }
```

### Types

#### Type Alias `LineIndex`

Zero-based index of a line of input as it is read from `stdin`.

`Iterator::enumerate` produces `usize`, and that is what scripts
see on stderr too. Centralising the type here lets us widen or
narrow it in one place if we ever need to.

```rust
pub type LineIndex = usize;
```

#### Enum `Error`

```rust
pub enum Error {
    CheckDigit {
        line_number: usize,
        nhs_number: nhs_number::NHSNumber,
    },
    Parse {
        line_number: usize,
        line: String,
        error: String,
    },
    Io {
        line_number: usize,
        error: String,
    },
}
```

##### Variants

###### `CheckDigit`

Fields:

| Name | Type | Documentation |
|------|------|---------------|
| `line_number` | `usize` |  |
| `nhs_number` | `nhs_number::NHSNumber` |  |

###### `Parse`

Fields:

| Name | Type | Documentation |
|------|------|---------------|
| `line_number` | `usize` |  |
| `line` | `String` |  |
| `error` | `String` |  |

###### `Io`

Fields:

| Name | Type | Documentation |
|------|------|---------------|
| `line_number` | `usize` |  |
| `error` | `String` |  |

##### Implementations

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Display**
  - ```rust
    fn fmt(self: &Self, __formatter: &mut ::core::fmt::Formatter<''_>) -> ::core::fmt::Result { /* ... */ }
    ```

- **Error**
- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **Send**
- **Sync**
- **ToString**
  - ```rust
    fn to_string(self: &Self) -> String { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **UnsafeUnpin**
- **UnwindSafe**
### Functions

#### Function `check_lines`

Run the line-validation subcommand.

`column` selects a 1-based comma-separated field per row, or
`None` for whole-line input. `format` selects the wire format
of stderr diagnostics; `Format::Text` is the FR-10 stable
contract and the default.

```rust
pub fn check_lines(column: Option<usize>, format: crate::app::args::Format) { /* ... */ }
```

#### Function `emit_check_digit`

```rust
pub(in ::subcommands::check_lines) fn emit_check_digit(line_number: usize, nhs_number: &nhs_number::NHSNumber, format: crate::app::args::Format) { /* ... */ }
```

#### Function `emit_parse`

```rust
pub(in ::subcommands::check_lines) fn emit_parse(line_number: usize, line: &str, error: &str, format: crate::app::args::Format) { /* ... */ }
```

#### Function `emit_io`

```rust
pub(in ::subcommands::check_lines) fn emit_io(line_number: usize, error: &std::io::Error, format: crate::app::args::Format) { /* ... */ }
```

## Module `counts`

The `--counts` subcommand.

Read NHS Numbers one per line from `stdin`, classify each line,
and at EOF print a summary on `stdout`.

The text format (default) is four key/value rows:

```text
valid:       <n>
invalid:     <n>
parse-error: <n>
blank:       <n>
```

With `--format json` the summary is a single JSON object:

```text
{"valid":3,"invalid":1,"parse_error":1,"blank":2}
```

With `--format tsv` the summary is a header row followed by a
row of integer counts.

```rust
pub(crate) mod counts { /* ... */ }
```

### Functions

#### Function `counts`

```rust
pub fn counts(column: Option<usize>, format: crate::app::args::Format) { /* ... */ }
```

### Functions

#### Function `pick_column`

Pick a 1-based column from a comma-separated line.

Returns `None` if the line has fewer than `column` fields.
Splitting is on the literal byte `,`; quoted fields containing
commas are not supported (out of scope — NHS Numbers never
contain commas in any documented format).

Callers pass the whole input line if `column` is `None`.

```rust
pub(crate) fn pick_column(line: &str, column: usize) -> Option<&str> { /* ... */ }
```

#### Function `json_escape`

Minimal JSON string escaper. Returns the string wrapped in
double quotes with the standard JSON escape sequences applied.

Only escapes the characters JSON requires: `"`, `\`, and the
C0 control range (`U+0000`–`U+001F`). NHS Numbers and the
strings the binary normally formats are ASCII, but raw input
lines (in the `line` field of parse errors) can contain
anything, including embedded quotes and control characters.

```rust
pub(crate) fn json_escape(s: &str) -> String { /* ... */ }
```

#### Function `tsv_escape`

Sanitise a string for use as a TSV field: replace tab and
newline characters with spaces so they cannot interfere with
the field/row separators. Other control characters are left
alone; downstream tools that care should validate input
themselves.

```rust
pub(crate) fn tsv_escape(s: &str) -> String { /* ... */ }
```

## Module `types`

```rust
pub(crate) mod types { /* ... */ }
```

### Modules

## Module `list`

**Attributes:**

- `Other("#[attr = MacroUse {arguments: UseAll}]")`

```rust
pub(crate) mod list { /* ... */ }
```

### Types

#### Type Alias `List`

**Attributes:**

- `Other("#[allow(dead_code)]")`

`List` is our typical list collection.

This is implemented in Rust by using `Vec`.

Example:

```
let my_list: List<i32> = List::new();
```


```rust
pub type List<T> = std::vec::Vec<T>;
```

### Macros

#### Macro `list`

**Attributes:**

- `Other("#[allow(unused_macros)]")`

Create a typical list collection with elements.

Example:

```
let x: List<i32> = list!(1, 2);
assert!(x.contains(&1));
```

OPTIMIZE: add pre-allocation logic.


```rust
pub(crate) macro_rules! list {
    /* macro_rules! list {
    ( $( $x:expr ),* $(,)? ) => { ... };
} */
}
```

## Module `map`

**Attributes:**

- `Other("#[attr = MacroUse {arguments: UseAll}]")`

```rust
pub(crate) mod map { /* ... */ }
```

### Types

#### Type Alias `Map`

**Attributes:**

- `Other("#[allow(dead_code)]")`

`Map` is our typical map collection and is an ordered map.

This is implemented in Rust by using `BTreeMap`.

Example:

```
let my_map: Map<i32, i32> = Map::new();
```


```rust
pub type Map<K, V> = std::collections::BTreeMap<K, V>;
```

### Macros

#### Macro `map`

**Attributes:**

- `Other("#[allow(unused_macros)]")`

Create a typical map collection with elements.

Example:

```
let x: Map<i32, i32> = map!(
    1 => 2,
    3 => 4,
);
assert_eq!(x[1], 2);
```

OPTIMIZE: add pre-allocation logic.


```rust
pub(crate) macro_rules! map {
    /* macro_rules! map {
    ( $( $k:expr => $v:expr ),* ) => { ... };
} */
}
```

## Module `queue`

**Attributes:**

- `Other("#[attr = MacroUse {arguments: UseAll}]")`

```rust
pub(crate) mod queue { /* ... */ }
```

### Types

#### Type Alias `Queue`

**Attributes:**

- `Other("#[allow(dead_code)]")`

`Queue` is our typical queue collection and is a double-ended queue.

This is implemented in Rust by using `VecDeque`.

Example:

```
let my_queue: Queue<i32> = Queue::new();
```


```rust
pub type Queue<T> = std::collections::VecDeque<T>;
```

### Macros

#### Macro `queue`

**Attributes:**

- `Other("#[allow(unused_macros)]")`

Create a typical queue collection with elements.

Example:

```
let x: Queue<i32> = queue!(1, 2);
assert!(x.contains(&1));
```

OPTIMIZE: add pre-allocation logic.


```rust
pub(crate) macro_rules! queue {
    /* macro_rules! queue {
    ( $( $x:expr ),* ) => { ... };
} */
}
```

## Module `set`

**Attributes:**

- `Other("#[attr = MacroUse {arguments: UseAll}]")`

```rust
pub(crate) mod set { /* ... */ }
```

### Types

#### Type Alias `Set`

**Attributes:**

- `Other("#[allow(dead_code)]")`

`Set` is our typical set collection and is an ordered set.

This is implemented in Rust by using `BTreeSet`.

Example:

```
let my_set: Set<i32> = Set::new();
```


```rust
pub type Set<T> = std::collections::BTreeSet<T>;
```

### Macros

#### Macro `set`

**Attributes:**

- `Other("#[allow(unused_macros)]")`

Create a typical set collection with elements.

Example:

```
let x: Set<i32> = set!(1, 2);
assert!(x.contains(&1));
assert!(x.contains(&2));
```

OPTIMIZE: add pre-allocation logic.


```rust
pub(crate) macro_rules! set {
    /* macro_rules! set {
    ( $( $x:expr ),* ) => { ... };
} */
}
```

## Module `stack`

**Attributes:**

- `Other("#[attr = MacroUse {arguments: UseAll}]")`

```rust
pub(crate) mod stack { /* ... */ }
```

### Types

#### Type Alias `Stack`

**Attributes:**

- `Other("#[allow(dead_code)]")`

`Stack` is our typical stack collection.

This is implemented in Rust by using `Vec`.

Example:

```
let my_stack: Stack<i32> = Stack::new();
```


```rust
pub type Stack<T> = std::vec::Vec<T>;
```

### Macros

#### Macro `stack`

**Attributes:**

- `Other("#[allow(unused_macros)]")`

Create a typical stack collection with elements.

Example:

```
let x: Stack<i32> = stack!(1, 2);
assert!(x.contains(&1));
```

OPTIMIZE: add pre-allocation logic.


```rust
pub(crate) macro_rules! stack {
    /* macro_rules! stack {
    ( $( $x:expr ),* $(,)? ) => { ... };
} */
}
```

## Functions

### Function `main`

```rust
pub(crate) fn main() { /* ... */ }
```

